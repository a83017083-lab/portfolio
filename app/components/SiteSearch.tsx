"use client";
import {useEffect,useState} from "react";
import Link from "next/link";
import type {SiteContent} from "../../lib/content";
const pages=[{label:"Home",href:"/",words:"portfolio websites automations"},{label:"About",href:"/about",words:"builder process"},{label:"Work",href:"/work",words:"projects github case studies"},{label:"Stack",href:"/stack",words:"technology skills"},{label:"Services",href:"/services",words:"pricing packages websites automation"},{label:"Journal",href:"/blog",words:"blog notes"},{label:"FAQ",href:"/faq",words:"questions answers"},{label:"Contact",href:"/contact",words:"demo call inquiry email"}];
type Entry={label:string;href:string;words:string};
export default function SiteSearch({content}:{content:SiteContent}){const[open,setOpen]=useState(false),[query,setQuery]=useState("");useEffect(()=>{const onKey=(e:KeyboardEvent)=>{if((e.metaKey||e.ctrlKey)&&e.key.toLowerCase()==="k"){e.preventDefault();setOpen(x=>!x)}if(e.key==="Escape")setOpen(false)};window.addEventListener("keydown",onKey);return()=>window.removeEventListener("keydown",onKey)},[]);const entries:Entry[]=[...pages,...content.projects.map(p=>({label:`Project: ${p.name}`,href:"/work",words:`${p.name} ${p.desc} ${p.tags.join(" ")}`})),...content.services.map(s=>({label:`Service: ${s.title}`,href:"/services",words:`${s.title} ${s.desc} ${s.tags.join(" ")}`})),...content.posts.filter(p=>p.published&&(!p.publishAt||p.publishAt<=new Date().toISOString())).map(p=>({label:`Journal: ${p.title}`,href:"/blog",words:`${p.title} ${p.excerpt} ${p.body}`})),...content.faqs.filter(f=>f.published).map(f=>({label:`FAQ: ${f.question}`,href:"/faq",words:`${f.question} ${f.answer}`}))];const results=query.trim()?entries.filter(p=>`${p.label} ${p.words}`.toLowerCase().includes(query.toLowerCase())).slice(0,15):pages;return <><button className="v2-search-btn" aria-label="Search site" onClick={()=>setOpen(true)}>⌕ <span>Search</span> <kbd>⌘K</kbd></button>{open&&<div className="v2-search-overlay" onMouseDown={e=>{if(e.target===e.currentTarget)setOpen(false)}} role="dialog" aria-modal="true" aria-label="Site search"><div className="v2-search-panel"><input autoFocus type="search" placeholder="Search pages, projects and notes…" value={query} onChange={e=>setQuery(e.target.value)}/><button onClick={()=>setOpen(false)} aria-label="Close search">×</button><div>{results.map((r,i)=><Link href={r.href} key={`${r.label}-${i}`} onClick={()=>{setOpen(false);setQuery("")}}>{r.label}<span>↗</span></Link>)}{results.length===0&&<p>No match. Try another word.</p>}</div></div></div>}</>}
