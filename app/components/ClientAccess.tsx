"use client";
import {useEffect,useState} from "react";
type Status={name:string;project:string;status:string;update:string};
export default function ClientAccess(){const [code,setCode]=useState(""),[client,setClient]=useState<Status|null>(null),[error,setError]=useState("");async function load(){const r=await fetch("/api/client/status",{cache:"no-store"});if(r.ok){const d=await r.json();setClient(d.client)}}useEffect(()=>{load()},[]);async function login(e:React.FormEvent){e.preventDefault();const r=await fetch("/api/client/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({code})});if(r.ok){setCode("");setError("");await load()}else setError("That access code wasn't recognized.")}return <main className="client-access"><div><a href="/">← Build With Abhinav</a><h1>Private project status</h1>{client?<><p>Welcome, {client.name}.</p><h2>{client.project}</h2><p><b>Status:</b> {client.status}</p><p>{client.update}</p><button onClick={async()=>{await fetch("/api/client/logout",{method:"POST"});setClient(null)}}>Sign out</button></>:<form onSubmit={login}><p>Enter the access code shared with you directly. This page never shows another client's information.</p><label>Access code <input type="password" value={code} onChange={e=>setCode(e.target.value)} required autoComplete="off"/></label><button>View project status</button>{error&&<p role="alert">{error}</p>}</form>}</div></main>}
