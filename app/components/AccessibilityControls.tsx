"use client";
import { useEffect,useState } from "react";
export default function AccessibilityControls(){const[large,setLarge]=useState(false),[contrast,setContrast]=useState(false);useEffect(()=>{setLarge(localStorage.getItem("site-large-text")==="1");setContrast(localStorage.getItem("site-high-contrast")==="1")},[]);useEffect(()=>{document.documentElement.classList.toggle("v2-large-text",large);document.documentElement.classList.toggle("v2-high-contrast",contrast);localStorage.setItem("site-large-text",large?"1":"0");localStorage.setItem("site-high-contrast",contrast?"1":"0")},[large,contrast]);return <div className="v2-access"><button aria-label={large?"Use standard text size":"Use larger text"} aria-pressed={large} title="Larger text" onClick={()=>setLarge(!large)}>A+</button><button aria-label={contrast?"Turn off high contrast":"Use high contrast"} aria-pressed={contrast} title="High contrast" onClick={()=>setContrast(!contrast)}>◐</button></div>}
