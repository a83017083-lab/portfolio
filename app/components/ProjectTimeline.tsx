import type {Project} from "../../lib/content";
export default function ProjectTimeline({projects}:{projects:Project[]}){return <section className="v2-section v2-container"><span className="v2-eyebrow">PROJECT INDEX / PUBLIC REPOS</span><h2>Builds in the open.</h2><p className="v2-timeline-note">A project index, not a dated history. Repository links show the source; publication dates and outcomes are not assumed.</p><ol className="v2-timeline">{projects.map((p,i)=><li key={p.name+i}><span>{String(i+1).padStart(2,"0")}</span><div><h3>{p.name}</h3><p>{p.cat} · {p.tags.join(" / ")}</p></div><a href={p.href} target="_blank" rel="noopener noreferrer">Source ↗</a></li>)}</ol></section>}
