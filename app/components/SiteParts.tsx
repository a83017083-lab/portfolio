import Link from "next/link";
import type { Project, Service } from "../../lib/content";
export function Eyebrow({ n, children }: { n: string; children: React.ReactNode }) { return <span className="v2-eyebrow"><span>{n}</span> / {children}</span>; }
export function PageIntro({ n, label, title, description }: { n: string; label: string; title: string; description: string }) { return <section className="v2-page-intro v2-container"><Eyebrow n={n}>{label}</Eyebrow><h1>{title}</h1><p>{description}</p></section>; }
export function ProjectCard({ project: p, index }: { project: Project; index: number }) { return <a className="v2-project" href={p.href || "/work"} target="_blank" rel="noopener noreferrer"><div className="v2-project-image"><img src={p.img} alt="" loading="lazy"/><span>View code ↗</span></div><div className="v2-card-meta"><span>{String(index + 1).padStart(2, "0")} / {p.cat}</span><span>↗</span></div><h3>{p.name}</h3><p>{p.desc}</p><div className="v2-tags">{p.tags.map(t => <span key={t}>{t}</span>)}</div></a>; }
export function ServiceCard({ service: s, index }: { service: Service; index: number }) { return <article className="v2-service"><div className="v2-service-top"><span>{String(index + 1).padStart(2, "0")}</span><span>✳</span></div><h3>{s.title}</h3><p>{s.desc}</p><div className="v2-tags">{s.tags.map(t => <span key={t}>{t}</span>)}</div></article>; }
export function CtaBand() { return <section className="v2-cta"><div className="v2-container"><Eyebrow n="NEXT">YOUR MOVE</Eyebrow><div className="v2-cta-row"><h2>Let&apos;s make something<br/><em>worth using.</em></h2><Link className="v2-round-link" href="/contact" aria-label="Start a project">↗</Link></div><p>Have a project in mind? Tell me what you&apos;re trying to solve.</p></div></section>; }
