"use client";
import {useState} from "react";
export default function NewsletterForm(){const[state,setState]=useState("idle");async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const data=new FormData(e.currentTarget);setState("sending");try{const res=await fetch("/api/newsletter",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email:data.get("email"),consent:data.get("consent")==="on",company:data.get("company")})});setState(res.ok?"done":"error")}catch{setState("error")}}return <form className="v2-newsletter" onSubmit={submit}><h3>Notes from the workbench</h3><p>Join the list for future project notes. No email updates are being sent yet. Ask to be removed any time.</p>{state==="done"?<p>Thanks, you&apos;re on the list.</p>:<><label>Email <input name="email" type="email" required placeholder="you@example.com"/></label><label className="v2-consent"><input name="consent" type="checkbox" required/> I agree to receive future project updates by email.</label><input name="company" tabIndex={-1} autoComplete="off" className="hp" aria-hidden="true"/><button className="v2-button" disabled={state==="sending"}>{state==="sending"?"Joining…":"Join the list →"}</button>{state==="error"&&<p role="alert">Couldn&apos;t save your signup. Try again later.</p>}</>}</form>}
