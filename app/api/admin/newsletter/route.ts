import { NextResponse } from "next/server";
import { isAuthed } from "../../../../lib/admin-guard";
import { kvListResult } from "../../../../lib/kv";
type Subscriber={email:string;joinedAt:number;consent:boolean};
export async function GET(){if(!isAuthed())return NextResponse.json({error:"Unauthorized"},{status:401});const result=await kvListResult<Subscriber>("newsletter:subscribers",0,1999);if(!result.ok)return NextResponse.json({error:"Storage unavailable"},{status:503});return NextResponse.json({subscribers:result.items},{headers:{"Cache-Control":"no-store"}});}
export async function DELETE(req:Request){if(!isAuthed())return NextResponse.json({error:"Unauthorized"},{status:401});const {email}=await req.json().catch(()=>({}));if(typeof email!=="string")return NextResponse.json({error:"Bad email"},{status:400});const result=await kvListResult<Subscriber>("newsletter:subscribers",0,1999);if(!result.ok)return NextResponse.json({error:"Storage unavailable"},{status:503});const all=result.items;const normalized=email.toLowerCase().trim();const filtered=all.filter(s=>s.email!==normalized);if(all.length===filtered.length)return NextResponse.json({error:"Not found"},{status:404});const {kvReplaceList}=await import("../../../../lib/kv");if(!await kvReplaceList("newsletter:subscribers",filtered))return NextResponse.json({error:"Storage failure"},{status:503});return NextResponse.json({ok:true});}
